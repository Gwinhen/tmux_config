# -- display -------------------------------------------------------------------

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

# setw -g automatic-rename off  # close renaming window
# set -g allow-rename off       # close renaming window
set -g renumber-windows on    # renumber windows when a window is closed
# 
# set -g set-titles on          # set terminal title
# 
# set -g display-panes-time 800 # slightly longer pane indicators display time
# set -g display-time 1000      # slightly longer status messages display time
# 
# set -g status-interval 10     # redraw status line every 10 seconds

# activity
set -g monitor-activity on
set -g visual-activity off
set -g mouse on

# -- status bar -------------------------------------------------------------------

# move status line to top
set -g status-position top

# Refresh status line every 5 seconds - Good for when music is playing / update time etc
set -g status-interval 1

# Length of tmux status line
set -g status-left-length 30
set -g status-right-length 150

set-option -g status "on"

# Default statusbar color
set-option -g status-style bg=colour237,fg=colour223 # bg=bg1, fg=fg1

# Default window title colors
set-window-option -g window-status-style bg=colour178,fg=colour237 # bg=yellow, fg=bg1

# Default window with an activity alert
set-window-option -g window-status-activity-style bg=colour237,fg=colour248 # bg=bg1, fg=fg3

# Active window title colors
set-window-option -g window-status-current-style bg=red,fg=colour237 # fg=bg1

# Set active pane border color
set-option -g pane-active-border-style fg=colour68

# Set inactive pane border color
set-option -g pane-border-style fg=colour247

# Message info
set-option -g message-style bg=colour239,fg=colour223 # bg=bg2, fg=fg1

# Writing commands inactive
set-option -g message-command-style bg=colour239,fg=colour223 # bg=fg3, fg=bg1

# Pane number display
set-option -g display-panes-active-colour colour1 #fg2
set-option -g display-panes-colour colour237 #bg1

# Clock
set-window-option -g clock-mode-colour colour109 #blue

# Bell
set-window-option -g window-status-bell-style bg=colour167,fg=colour235 # bg=red, fg=bg

set-option -g status-left "\
#[fg=colour7, bg=colour241]#{?client_prefix,#[bg=colour167],} ❐ #S \
#[fg=colour241, bg=colour237]#{?client_prefix,#[fg=colour167],}#{?window_zoomed_flag, 🔍,} "
#[fg=colour178]#(echo "$USER")@#(cat /proc/sys/kernel/hostname) "

# set-option -g status-right "\
# #[fg=colour178, bg=colour237]  #($HOME/.tmux/gpu.sh) \
# #[fg=colour68, bg=colour237]  #(curl -s wttr.in/\?format\="+%%c%%20%%t%%60%%w&period=60") \
# #[fg=colour248, bg=colour239]"

set-option -g status-right "\
#[fg=colour178, bg=colour237]  #($HOME/.tmux/gpu.sh) \
#[fg=colour248, bg=colour239]"

set-window-option -g window-status-current-format "\
#[fg=colour237, bg=colour178]\
#[fg=colour239, bg=colour178] #I* \
#[fg=colour239, bg=colour178, bold] #W \
#[fg=colour178, bg=colour237]"

set-window-option -g window-status-format "\
#[fg=colour223,bg=colour237] #I \
#[fg=colour223, bg=colour237] #W "
